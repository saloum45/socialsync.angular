import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditTypeSocialAccountsComponent } from './edit-type-social-accounts.component';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting } from '@angular/common/http/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('EditTypeSocialAccountsComponent', () => {
  let component: EditTypeSocialAccountsComponent;
  let fixture: ComponentFixture<EditTypeSocialAccountsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
       providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        NgbActiveModal
      ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(EditTypeSocialAccountsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
